[package]
name = "id_card_compositor"
version = "2.0.0"
edition = "2021"

[dependencies]
image = "0.25"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
log = "0.4"
env_logger = "0.11"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
